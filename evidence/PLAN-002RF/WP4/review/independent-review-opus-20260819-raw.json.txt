```json
{
  "verdict": "NEEDS_REWORK",
  "cross_provider": true,
  "security_concerns": [
    "Resource amplification within the declared caps: the low-contrast / clutter guards bound ink *fraction*, never absolute work. MAX_SOURCE_PIXELS=100,000,000 and INK_FRACTION_BAND=(0.001, 0.35) admit up to 35M ink pixels, after which (a) G.hough_physical_lines runs a pure-Python per-ink-pixel loop, each iteration a 720-wide NumPy op, (b) _detect_arc_from_structural runs a fixed 20,000 RANSAC iterations each scoring O(n_ink/4) points and _detect_diagonal_from_structural 5,000 over all n_ink, (c) _paint_arc_ring / _paint_line_band allocate two full h*w np.mgrid index grids (~1.6 GB at 100 MP) on top of the label/coverage arrays. A single in-band 10000x10000 PNG at ~5% ink is a local CPU/OOM denial of service. config.PARSER_TIMEOUT_S / MAX_WORKER_STDIO_BYTES exist but extract_raster_auto is called in-process by parse_raster_auto with no timeout, ink-count cap or iteration budget. Add an absolute ink-pixel/work cap that fires with a PARSE_RESOURCE_LIMIT finding in the same short-circuit block as the clutter guard.",
    "Absolute local filesystem path leaked into a persisted finding: _decode returns source_ref=str(path) for the 'source file not found' PARSE_SOURCE_UNSUPPORTED case while every other finding deliberately uses path.name. Findings are serialized into evidence artifacts under a repository with a public remote, disclosing the operator's directory layout. Use path.name consistently.",
    "_load_authoritative_anchors reads the sibling manifest with an uncapped read_text() and json.loads before any size check (the raster gets MAX_SOURCE_RASTER_BYTES, the manifest gets nothing), so an oversized sibling *-scale-anchors.json is fully materialized. No path traversal exists (the manifest path is derived from the raster via with_name) and the SHA-256 binding + fail-closed empty-anchor return are correct; only the missing size cap needs fixing."
  ],
  "logic_errors": [
    "opening.wall_id is emitted as the worker's INTEGER wall index, but schemas/floorplan_parse/v1/floorplan_parse-1.2.0.schema.json requires openings[].wall_id as a required non-empty STRING, and the emitted wall ids are 'w-%04d'. Every real emit that contains openings (FX1 emits 6) is therefore schema-invalid AND carries a reference that matches no wall id in the payload. This is invisible to the suite because test_emit_raster_auto_parse_is_schema_valid passes openings=[] and the passage test asserts only that the payload is emptied. Fix: emit f\"w-{wall_id:04d}\" (and add a schema test with a non-empty openings list).",
    "Fail-closed does NOT hold on the worker channel in general. extract_raster_auto returns full walls/rooms/openings ALONGSIDE the late blocking findings (SCALE_ANCHORS_INSUFFICIENT, PARSE_DIMENSION_INCONSISTENT, RASTER_OVERSEGMENTED, RASTER_UNEXPLAINED_INK); only the early decode/contrast/clutter returns are empty. main() then json-dumps that dict verbatim to stdout or an output file, so any consumer of the worker CLI (the subprocess shape implied by MAX_WORKER_STDIO_BYTES / PARSER_TIMEOUT_S) receives geometry next to a blocking finding. Only parse_raster_auto enforces W-17/AT-18. Empty the geometry in the worker itself (or make main() refuse) so the property does not depend on which entry point a caller picks.",
    "The emitter's span guard covers only kind=='passage'. door and window widths are emitted unchecked, although the frozen taxonomy bounds every opening span (metrics.MAX_OPENING_SPAN_MM = 1500). A 6 m 'window' is emitted as product geometry with no blocking finding; the already-documented O-W3 width error (0.365 m emitted vs 1.2 m authored) likewise passes silently. Check every opening span against the frozen bound.",
    "Arc angles can never satisfy the truth convention and are emitted out of convention. _finalize_units computes start_deg = -(start_img % 360.0) and end_deg = -(end_img % 360.0), always in (-360, 0], while fx1-truth.json W-APSE is start_deg=-90 / end_deg=+90. Exact-by-key matching therefore fails on the arc regardless of fit quality (RUN-REPORT concedes 'the angle parametrization differs modulo 360'), and a downstream consumer receives e.g. start_deg=-270 for a +90 arc. Normalize both angles into the frozen (-180, 180] range.",
    "The arc bulge/sweep invariant is vacuous by construction. _finalize_units hardcodes sweep='ccw' and derives bulge from CG.bulge_for_sweep('ccw', ...), so recognition.arc_invariants' sign check compares two values the same line produced and can never detect a direction error — even though the adjacent comment states the y-flip reverses the traversal direction. Derive sweep from the detected traversal direction and let the invariant do real work.",
    "Window detection is coupled to the fixture's renderer, not to a geometric property. _glazing_at tests the single hardcoded offset (x+4, y+4), i.e. the authoring offset of the FX1/corpus writer, and the docstring concedes that any other offset over-detects. A window whose glazing is offset toward the opposite side of its host wall is undetectable. Same class: arc radius band 150-450 px, RANSAC spread 60 px, cnt >= 300 / >= 75, leaf run 100/190 px, glazing radius r-8. This contradicts raster_auto_geometry's 'none is tuned against truth (anti-gaming: AT-18, AT-21)' claim, and it means FX1/corpus convergence does not evidence generalization.",
    "The two-anchor (here three-anchor) scale validation cannot fail. _load_authoritative_anchors reads BOTH real_length_m and span_px out of the manifest (which also ships derived_m_per_px = 0.005); nothing about the anchor is measured from the raster. G.fit_scale's median_residual, disagreement and min_anchor_span_px therefore compare author-declared numbers against themselves — residual and disagreement are 0 by construction and the AT-15 gates are unreachable. A wrong anchor is undetectable, so 'two-anchor scale validation: met' is not supported. The anchor's pixel span must be measured from the anchor ink (the anchors are drawn at value 64 and are already excluded from the structural mask, so the measurement path exists) with the manifest supplying only the real length.",
    "The scorer matches against a projection of the truth chosen by the recognizer's own author, not against the authored truth record: _truth_mm_record strips orientation ('diagonal_3_4_5' on W-DIAG), tessellation_rule and vertices_mm before hashing, and canonical_key excludes only {id, confidence, width_mm}. So 'the frozen WP1 evaluator's exact-by-key canonical matcher' is in fact applied to a reduced shape defined in raster_auto.py, which relaxes what a prediction must reproduce. Stripping tessellation_rule additionally makes support_taxon_supported fall back to the default sagitta bound, so an out-of-taxon truth arc is counted as supportable. The projection belongs in the evaluator, and both sides must be projected identically.",
    "truth_record_from_wall's a/b canonicalization is sound but ASYMMETRIC and rests on an unenforced assumption. It cannot falsely match (two segments with the same endpoint pair are the same segment) and cannot drop a correct wall for the current data — I verified all 8 fx1-truth.json segments and the f37 corpus truth are authored lexicographically-smaller-endpoint-first. But the normalization is applied only to the prediction; _truth_mm_record passes truth a_mm/b_mm through untouched, so the first truth wall authored in the other order becomes structurally unmatchable with no diagnostic. Canonicalize inside _truth_mm_record too (or assert the invariant on load). Note also that the canonicalization lives in the scorer only — the emitted payload still carries the detector's raw endpoint order.",
    "The emitted payload always sets scale_m_per_px: None even when the worker resolved 0.005 m/px, and parse_raster_auto computes mm_per_px and hands it to _wall_to_metres / _room_to_metres / _opening_to_metres, all three of which ignore the parameter entirely. The validated scale is silently dropped from the product artifact and raw['frame'] is discarded."
  ],
  "suggestions": [
    "Short-circuit ordering (focus 2) is CORRECT as written: the RASTER_LOW_CONTRAST and RASTER_CLUTTER_EXCEEDS_ENVELOPE findings both precede G.connected_components, wall_centerlines, Hough and both RANSACs, and the byte/pixel/format/mode guards all run header-only before .load(). Only note: _to_l materializes the whole decoded array and binarize_structural runs before the early return, so the byte and pixel caps are the only true pre-allocation bound (see the resource-amplification item).",
    "Determinism (focus 3) is clean: both RANSACs use np.random.RandomState with fixed seeds (the legacy generator's stream is stability-guaranteed by NumPy policy), Hough peak selection breaks ties via np.argmax plus a stable sort, the duplicate scan and room walk iterate lists and insertion-ordered dicts, and every float aggregate is over a deterministically-ordered array. Two latent hazards worth closing: _derive_rooms reads out[cv] on a defaultdict while iterating out.items() (safe only because every cv is already an inserted key — one graph change from RuntimeError: dictionary changed size during iteration), and best is dereferenced without a None check. Use a plain dict and guard best.",
    "_paint_segment_coverage is dead code (nothing calls it) — delete it.",
    "render_raster_auto_svg emits no width/height/viewBox and negates y, so every element sits at negative y outside the default viewport: the evidence overlay renders blank in a standard renderer. The arc's large-arc-flag is also hardcoded 0, so any sweep > 180 deg draws the wrong side. Emit a viewBox computed from the geometry bounds and derive the large-arc flag from the sweep.",
    "_segments_openings marks out-of-image centreline positions as empty, so a wall that reaches the raster border yields a phantom gap/opening. Treat out-of-bounds as occupied or clip the scan to the image.",
    "score_against_truth is greedy first-fit rather than maximum bipartite matching, so it can undercount when two truth walls compete for one prediction. Deterministic, but report it as a lower bound or use a real assignment.",
    "_decode's return annotation says 'dict | None' for the third element while it actually returns 'list[dict] | None'.",
    "Housekeeping from this review: I created D:\\משה פרוייקטים\\פיתוח אתרים\\PanoWorld-Automation\\.tmp_arccheck.py to execute the FX1 arc/score path, but both the run and the delete were blocked by permissions in this non-interactive session, so the analysis above is static-only and that scratch file needs manual deletion before the next commit."
  ],
  "summary": "Fail-closed holds at the emitter and the short-circuit ordering and determinism are sound, but the emitted openings violate the 1.2.0 schema (integer wall_id, dangling reference), the worker channel and its CLI still return geometry alongside late blocking findings, door/window spans are unchecked, arc angles and bulge/sweep are emitted out of convention with a vacuous invariant, and scale 'validation' plus window detection are read from author-declared manifest values and the fixture's render offset rather than measured from the raster."
}
```
