{"verdict":"APPROVE_WITH_FIXES","findings":[{"severity":"MAJOR","file":"src/pwa/floorplan/cad_exact_geometry.py","line":1,"message":"bulge_magnitude(sweep_rad) uses tan(theta/4) but sweep_rad is in radians; theta in (0,2pi) implies sweep_rad is degrees? Inconsistent units: if sweep_rad is radians, tan(sweep_rad/4) is correct, but the comment says theta in (0,2pi) which is radians; however sweep_from_bulge returns 4*atan(|bulge|) in radians, so bulge_magnitude should accept radians. Verify all callers pass radians consistently; otherwise sign/magnitude errors."},{"severity":"MAJOR","file":"src/pwa/floorplan/cad_exact_worker.py","line":1,"message":"Unit conversion: $INSUNITS maps 4->mm,5->cm,6->m, but the emitter comment says 'inputs are METRES' and quantizes to 0.01mm grid. If DXF units are mm/cm, the worker must convert to metres before emitting; otherwise the canonical_key (mm) will be off by 10x/100x. No explicit conversion factor is shown in the worker extraction logic."},{"severity":"MAJOR","file":"src/pwa/floorplan/cad_exact_geometry.py","line":1,"message":"sweep_from_endpoints(start,end,dir): ccw delta=(end-start)60; cw=(360-delta)60. The '60' multiplier is unexplained and likely wrong — if start/end are angles in degrees, delta should be (end-start) mod 360, not multiplied by 60. This will produce sweep angles 60x too large, breaking sagitta and tessellation."},{"severity":"MAJOR","file":"src/pwa/floorplan/cad_exact_worker.py","line":1,"message":"Full-circle arc rejection: The worker rejects full-circle ARC, but the review rules require fail-closed on 'full-circle arc'. However, the geometry module has is_bounded_circular_arc which checks finite/radius>0 but does not explicitly reject sweep==2*pi. The worker must check sweep magnitude < 2*pi (or endpoints not coincident) to block full circles; otherwise a full-circle ARC with start==end may pass as a zero-length arc."},{"severity":"MAJOR","file":"src/pwa/floorplan/cad_exact_worker.py","line":1,"message":"Passage span check: 'passage width > 3.0' blocks, but the rule says PASSAGE_SPAN_MAX_M = 3.0 and 'passage span > 3.0 m (block)'. The worker must compare in metres after unit conversion. If the LINE endpoints are in DXF units (mm/cm), the width must be converted to metres first; otherwise a 3.1m passage in mm (3100) would be compared as 3100 > 3.0 and incorrectly blocked, or a 3.1m in cm (310) also wrong."},{"severity":"MAJOR","file":"src/pwa/floorplan/cad_exact_worker.py","line":1,"message":"Degenerate wall check: DEGENERATE_WALL_M = 0.05 m. The worker must convert the LINE length from DXF units to metres before comparing. If the length is in mm, a 0.04m wall (40mm) would be compared as 40 < 0.05 and incorrectly blocked, or a 0.06m wall (60mm) would pass incorrectly. Unit conversion must happen before all geometric checks."},{"severity":"MINOR","file":"src/pwa/floorplan/cad_exact_geometry.py","line":1,"message":"min_segments_for_sagitta uses 'smallest power-of-two N>=2' but sagitta formula uses segment=theta/(2N). The sagitta bound SAGITTA_MAX_PX=0.5 is in pixels, but the function takes mm_per_px. Verify that radius_mm is in millimetres and mm_per_px is consistent; if radius is in metres, the sagitta will be 1000x too large, causing over-tessellation and potential entity count blowup."},{"severity":"MINOR","file":"src/pwa/floorplan/cad_exact_worker.py","line":1,"message":"Duplicate wall detection: The rule requires blocking duplicate walls, but the worker description only mentions 'duplicate wall => PARSE_DUPLICATE_ENTITY'. Need to verify the duplicate check is exact (same endpoints, same thickness) and not approximate (float tolerance), to maintain determinism. If using float equality, nondeterminism could arise from different parsing paths."},{"severity":"MINOR","file":"src/pwa/floorplan/cad_exact_worker.py","line":1,"message":"Self-intersecting room: The worker checks self-intersection for LWPOLYLINE, but the rule also requires 'open polygon' blocking. The worker mentions '<3 vertices => PARSE_OPEN_POLYGON', but a closed polygon with 3+ vertices that is not actually closed (first != last) may not be caught. Verify the closed flag and first/last point equality are both checked."},{"severity":"MINOR","file":"src/pwa/floorplan/cad_exact.py","line":1,"message":"Emitter quantizes to 0.01mm grid, but the evaluator QUANTIZE_MM = 0.01. The emitter comment says 'inputs are METRES' and quantizes to 0.01mm — this means converting metres to mm then rounding to 0.01mm. Verify the rounding is deterministic (e.g., round-half-even or floor) and not subject to float representation issues (e.g., 0.1+0.2)."},{"severity":"MINOR","file":"src/pwa/floorplan/cad_exact_worker.py","line":1,"message":"Security refusal: _SECURITY_UNSUPPORTED_KINDS includes SPLINE, INSERT, IMAGE, OLE2FRAME, but the rule also mentions 'sourced thickness (missing => block)'. The worker blocks missing thickness for PWA-WALL, but what about ARC walls? The description says ARC => circular_arc with sourced thickness; if ARC has no thickness, it must also block. Verify ARC thickness is checked."},{"severity":"INFO","file":"src/pwa/floorplan/cad_exact_worker.py","line":1,"message":"Additive-only verification: The historical dxf_worker rejects ARC/bulge. The new worker is separate (cad_exact_worker), but the test section G mentions 'migration (historical dxf still rejects arc/bulge)'. Verify that the historical worker's code is not modified and that the new worker is only invoked when explicitly requested (routes default-off). No activation path should exist in the historical path."},{"severity":"INFO","file":"src/pwa/floorplan/cad_exact_geometry.py","line":1,"message":"tessellate_arc uses n+1 vertices with ccw->+step, cw->-step. Verify that for a full-circle arc (if allowed) the last vertex equals the first, but since full-circle is rejected, this is fine. However, for a near-full-circle (sweep close to 2pi), the step calculation must not overflow or produce duplicate vertices due to float rounding."},{"severity":"INFO","file":"src/pwa/floorplan/cad_exact.py","line":1,"message":"resolve opening host by span collinearity / radial distance for arcs: This uses geometric matching which may introduce float nondeterminism. The rule requires exact/deterministic output. Verify that the host resolution uses quantized coordinates (0.01mm) and not raw floats, to ensure the same input always produces the same output."}]}